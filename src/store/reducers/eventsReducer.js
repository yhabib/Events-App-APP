// import { SET_CURRENT_USER } from './../actions/currentUser';

/*
state = {
  currentUser: {
    userName,
    avatar,
    token,
  },

};
*/


export default function(state = {}, action) {  
  switch (action.type) {
    
    default:
      return state;
  }
};